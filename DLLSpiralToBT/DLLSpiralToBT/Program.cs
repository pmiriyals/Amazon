using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DLLSpiralToBT
{
    //Given a dl representing the spiral level order traversal of a binary tree,convert it to a 
    //binary tree using one stack. In Last level, nodes will be either to the right or left only. 
    //complete code in C. It is usually done using two stacks. Can it be done using one stack?
    
    class Program
    {
        static void Main(string[] args)
        {
        }
    }
}
